"""Live broker adapters. Each adapter implements the FillModel interface
so the runtime can swap between simulation and real execution by changing
ONE argument.

Currently:
  - tradovate.py: stub + planned implementation for Tradovate REST + WS
"""
