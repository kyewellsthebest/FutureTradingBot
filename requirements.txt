numpy>=1.26
pandas>=2.1
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3
yfinance>=0.2.40
flask>=3.0
flask-cors>=4.0
requests>=2.31
plotly>=5.20
gunicorn>=21.2
websocket-client>=1.7
orjson>=3.10
# parquet engine -- the researcher's deep tiers (data/research_bars,
# data/depth) are parquet. Without this, tier 2 and tier 3 raise on
# read and the searcher silently runs on tier 1 alone.
pyarrow>=15.0
